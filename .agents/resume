#!/usr/bin/env bash
set -euo pipefail

[[ "$(node -p 'process.versions.node.split(".")[0]')" == 24 ]]
[[ "$(pnpm --version)" == 10.18.0 ]]
command -v supabase >/dev/null
command -v semgrep >/dev/null

printf 'Orb toolchain ready: Node %s, pnpm %s\n' "$(node --version)" "$(pnpm --version)"
